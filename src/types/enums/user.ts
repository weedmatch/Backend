export enum UserType{
    INDIVIDUAL = "Individual",
    ADMIN = "Admin",
    SUBADMIN = "SubAdmin",
}

export enum UserStatus {
    ACTIVE = 'Active',
    INACTIVE = 'Inactive',
    DELETED = 'Deleted',
}
