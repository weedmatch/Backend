export default class TranactionService {
    
    static async getAll(){
    }

    static async getOne(
        id: string
    ){
    }

    static async createTransaction(
        userId: string, 
    ){
    }

    //TODO: query for a transaction
    static async queryTransaction() {
    }

    static async updateTransaction(
        id: string, 
    ) {
    }

}